nextParticle = new NextParticle(
    nextParticle = new NextParticle({
        image: document.all.logo,
        addTimestamp: true,
        initPosition: 'none',
        initDirection: 'none',
        
      })
);